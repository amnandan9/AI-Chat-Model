import streamlit as st
import google.generativeai as genai

# Configure the API key
API_KEY = "AIzaSyB1WeGKC0OhvtAwislWO1gfJX4pyMl58B0"
genai.configure(api_key=API_KEY)

# Set up the Gemini model
generation_config = {
    "temperature": 1,
    "top_p": 0.95,
    "top_k": 40,
    "max_output_tokens": 8192,
    "response_mime_type": "text/plain",
}

model = genai.GenerativeModel(
    model_name="gemini-1.5-flash",
    generation_config=generation_config,
)

# Function to generate responses
def generate_response(input_text):
    response = model.generate_content([
        "Nandan created you as his personal software-knowledgeable friend or buddy. You are not related to Google — don't mention Google.",
        f"input: {input_text}",
        "output: ",
    ])
    return response.text

# Streamlit interface
st.title("🧑‍💻 Skolo Online 💬 Chatbot")

st.write("Hi🤖! How can I assist you today?")

# Text input for user message
user_input = st.text_input("Your message:")

if st.button("Send") and user_input:
    bot_response = generate_response(user_input)
    st.markdown("**Bot:** " + bot_response)
